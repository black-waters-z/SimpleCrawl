## 阅读指南

**创建并激活虚拟环境**

```
python -m venv venv
source venv/bin/activate # macOS/Linux
\venv\Scripts\activate # Windows
```

**安装依赖**

```
pip install -r requirements.txt
```

**命令行运行**

```
python main.py
```
